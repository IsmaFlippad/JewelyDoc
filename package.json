{
  "scripts": {
    "build": "npm run build --prefix app/frontend && npm run build --prefix app",
    "start": "npm run start --prefix app",
    "dev": "npm run dev --prefix app",
    "deploy": "vercel --prod"
  }
}