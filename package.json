{
  "name": "jewely-docs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix app",
    "start": "npm start --prefix frontend",
    "dev": "npm run dev --prefix app",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">= 20.0.0"
  }
}